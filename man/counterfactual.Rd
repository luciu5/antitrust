% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Counterfactual.R
\name{counterfactual}
\alias{counterfactual}
\title{Define a post-calibration economic counterfactual}
\usage{
counterfactual(
  ownership = NULL,
  costs = NULL,
  exit = NULL,
  capacity = NULL,
  tariff = NULL,
  quota = NULL,
  bargaining = NULL,
  leader = NULL,
  products = NULL,
  quality = NULL,
  entry = NULL,
  ...
)
}
\arguments{
\item{ownership}{Post-counterfactual ownership.}

\item{costs}{Post-counterfactual marginal-cost changes. Constant-cost
Bertrand-family models apply these to their retained structural cost
primitives; second-score Logit uses its legacy additive cost-level
convention.}

\item{exit}{Products to remove, or a logical active-product vector.}

\item{capacity}{Post-counterfactual capacities.}

\item{tariff}{A tariff change, unsupported by antitrust models.}

\item{quota}{A quota change, unsupported by antitrust models.}

\item{bargaining}{Post-counterfactual bargaining parameters.}

\item{leader}{Post-counterfactual leader indicators for Stackelberg models.}

\item{products}{Post-counterfactual product structure for Stackelberg models.}

\item{quality}{A named numeric vector of proportional changes to product
attractiveness, keyed by product label. For Logit demand this is a
proportional choice-weight change, represented as
`meanval_new <- meanval_current + log1p(quality)`; CES retains its
multiplicative mean-value convention.}

\item{entry}{An `Entrant` object, or a list of `Entrant` objects, each
describing a new single-product firm to add to the market.}

\item{...}{Reserved; model specification fields are rejected.}
}
\value{
A `Counterfactual` object with exactly one `CounterfactualStep`.
}
\description{
`counterfactual()` stores only supplied changes to the economic
environment, bundled as the single `CounterfactualStep` of a
`Counterfactual`. Demand, conduct, and model variants are deliberately
not counterfactual fields; use `update()` or `respecify()` for those
changes. Use `add_step()` to append further, sequential steps.
}
