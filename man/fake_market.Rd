% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SyntheticMarketDesign.R
\name{fake_market}
\alias{fake_market}
\title{Generate a reproducible synthetic antitrust market design}
\usage{
fake_market(
  mode = "observed",
  n_firms = 3L,
  n_products = 1L,
  dirichlet_alpha = NULL,
  outside_beta = c(2, 8),
  prices = NULL,
  reference_price = NULL,
  price_rule = c("common", "uniform"),
  price_level = 100,
  price_range = c(50, 150),
  outside_margin = NULL,
  observed_markup = NULL,
  reference_markup = NULL,
  markup_range = c(0, 100),
  parameters = list(),
  alpha = NULL,
  seed = NULL
)
}
\arguments{
\item{mode}{Either `"observed"` or `"primitives"`.}

\item{n_firms}{Number of inside firms; the reference firm is additional.}

\item{n_products}{Number of products owned by each inside firm. A scalar is
recycled across firms; a vector must have length `n_firms`.}

\item{dirichlet_alpha}{A positive finite vector with one element for each
inside product. If omitted, all product-level Dirichlet shapes equal one.}

\item{outside_beta}{A positive finite length-two vector of Beta shapes.}

\item{prices}{Optional positive prices for inside products or all products.}

\item{reference_price}{Price appended when only inside prices are supplied.}

\item{price_rule}{Either `"common"` or `"uniform"` when prices are not
supplied.}

\item{price_level}{Common positive price and default reference price.}

\item{price_range}{Two positive endpoints for the uniform price rule.}

\item{outside_margin}{Optional observed reference-product level margin.}

\item{observed_markup}{Backward-compatible alias for `outside_margin`.}

\item{reference_markup}{Backward-compatible alias for `outside_margin`.}

\item{markup_range}{Two endpoints for the observed markup draw.}

\item{parameters}{A list of model-specific known primitives in primitives
mode, such as `list(alpha = -1)`.}

\item{alpha}{Optional shorthand for `parameters$alpha`.}

\item{seed}{An optional explicit integer seed.}
}
\value{
A `SyntheticMarket` object.
}
\description{
The design layer draws shares, prices, ownership, and optionally one
observed reference-product markup. It does not infer demand or conduct
primitives. The active reference product is an additional one-product firm
and participates in the ownership matrix; its mean utility is normalized by
the economic realization adapter.
}
\details{
`n_products` may be a scalar, in which case all inside firms have the same
number of products, or a vector of length `n_firms`, in which case it gives
each firm's product count. Shares are drawn at the product level, so
`dirichlet_alpha` has one element for each inside product.
}
