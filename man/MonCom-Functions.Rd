% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MonComFunctions.R
\name{MonCom-Functions}
\alias{MonCom-Functions}
\alias{moncom.logit}
\alias{moncom.ces}
\title{Monopolistic-competition calibration}
\usage{
moncom.logit(
  prices,
  shares,
  margins,
  ownerPre,
  ownerPost,
  output = TRUE,
  weights = rep(1, length(shares)),
  normIndex = ifelse(isTRUE(all.equal(sum(shares), 1, check.names = FALSE)), 1, NA),
  mcDelta = rep(0, length(prices)),
  subset = rep(TRUE, length(prices)),
  insideSize = NA_real_,
  priceOutside = 0,
  priceStart = prices,
  isMax = FALSE,
  control.slopes,
  control.equ,
  labels = paste("Prod", 1:length(prices), sep = ""),
  ...
)

moncom.ces(
  prices,
  shares,
  margins,
  ownerPre,
  ownerPost,
  output = TRUE,
  weights = rep(1, length(shares)),
  normIndex = ifelse(isTRUE(all.equal(sum(shares), 1, check.names = FALSE)), 1, NA),
  mcDelta = rep(0, length(prices)),
  subset = rep(TRUE, length(prices)),
  insideSize = NA_real_,
  priceOutside = 1,
  priceStart = prices,
  isMax = FALSE,
  control.slopes,
  control.equ,
  labels = paste("Prod", 1:length(prices), sep = ""),
  ...
)
}
\arguments{
\item{prices}{A length-k vector of observed product prices.}

\item{shares}{A length-k vector of quantity shares for Logit or revenue
shares for CES.}

\item{margins}{A length-k vector of observed positive margins. At least one
margin is required; additional margins are used as precision weights.}

\item{ownerPre}{Pre-merger ownership vector or matrix. It is retained as
model metadata but does not enter the MonCom FOC.}

\item{ownerPost}{Post-merger ownership vector or matrix.}

\item{output}{`TRUE` for an output market and `FALSE` for an input market.}

\item{weights}{A length-k vector of non-negative product weights used in
calibration.}

\item{normIndex}{Normalization product when inside shares exhaust the market.}

\item{mcDelta}{A vector of proportional marginal-cost changes.}

\item{subset}{A logical vector identifying products active in the post-
counterfactual market.}

\item{insideSize}{Total inside quantity (Logit) or revenue (CES).}

\item{priceOutside}{Outside-good price when the market has an outside good.}

\item{priceStart}{Starting values for the post-counterfactual price solver.}

\item{isMax}{If `TRUE`, request the legacy local-profit maximum diagnostic.}

\item{control.slopes}{Optional legacy calibration controls.}

\item{control.equ}{Optional legacy equilibrium-solver controls.}

\item{labels}{Product labels.}

\item{...}{Additional legacy model options.}
}
\value{
A `MonComLogit` or `MonComCES` object.
}
\description{
Calibrate flat Logit or CES demand under differentiated-product
monopolistic competition.  Each product uses its own perceived demand
derivative and does not internalize cross-product effects.  For CES, the
aggregate CES index is held fixed, so the perceived own elasticity is
`-gamma`, rather than the full share-adjusted CES elasticity.  The BLP
MonCom model is exposed through the general `calibrate()` and `specify()`
lifecycle using the validated integration engine.
}
